import { FilterCandidatPipe } from './filter-candidat.pipe';

describe('FilterCandidatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCandidatPipe();
    expect(pipe).toBeTruthy();
  });
});
