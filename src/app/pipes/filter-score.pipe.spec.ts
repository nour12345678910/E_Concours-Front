import { FilterScorePipe } from './filter-score.pipe';

describe('FilterScorePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterScorePipe();
    expect(pipe).toBeTruthy();
  });
});
